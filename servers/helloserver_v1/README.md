Simple Express app that responds with 'hello world' to HTTP GET requests.

```text
npm install
npm start
```

Using your browser, navigate to http://localhost:8080.

Or using curl:

```text
curl localhost:8080
```
